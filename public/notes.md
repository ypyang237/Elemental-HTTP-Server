Need to create template for elements and link it up to index.HTML

  - need to render all fields(variables like elementDescription) correctly

Also need to update number count on index.html

can modularize it